# 質問箱DB反映仕様書 v1.3
改定：2026-09-25（§6.10を新設。公開後に回答を訂正したときの後処理4点を定めた）
　　　2026-09-18（§6.9にrevise操作を新設。既存行の訂正・失効・重複削除の経路を開いた）
　　　2026-09-17（定期タスクをCodeのクラウドルーティンへ移行。承認後の反映をGitHub経由・GASの1時間ごとの巡回に変更）

note質問箱の**公開後**の工程——回答を断言台帳へ反映する自動取り込みと定期タスク——は本仕様に従う。
回答の制作工程（情報取得・回答の型・チェック・承認）は『質問箱ワークフロー仕様書』に定める。
本文中の「§6.x」は本仕様書内の該当節を指す。「§1」「§2」「§3」等は『質問箱ワークフロー仕様書』の該当節を指す。

改定履歴は『質問箱ワークフロー_改定履歴』に一本化してある。

---

## 6. データベースへの反映【v2.0全面改定】

2026-09-12にWeb App化した。メニュー操作によるテキストの書き出し・貼り付けは廃止。
2026-09-17に、定期タスクをCodeのクラウドルーティンへ移行した【v1.1】。
**CEOの操作は④の承認（ルーティンの会話画面で「承認」と返信する）のみである。**

### 6.1 全体の流れ

```
① note質問箱に回答を公開
        ↓  翌朝4時台・全自動（Google Apps Script）
② 「質問箱」タブに行が追加される
     No／質問URL／質問文／回答全文／公開日／処理状態=未処理
     カテゴリ（E列）とパーツ列（F〜K列）は空のまま
        ↓  毎日9時にCodeのクラウドルーティン「質問箱 断言台帳 反映」が起動【v1.1改定】
③ ルーティンが ?type=qabox_pending で未処理分を自律取得する
   （前回確認からの経過日数に関わらず、未処理分を全件まとめて取得する）
        ↓
③.5 各バッチの開始時に、規範文書の版を再確認する【v2.8新設】
   `kamen-a-rules\specs\質問箱DB反映仕様書.md`（本仕様書）の本文1行目を読み、
   前バッチから版が変わっていれば改定内容を確認してから分類に入る【v2.10改定：分割に伴い参照先を本仕様書へ変更】。
   契機：2026-09-15、バッチ処理中に仕様書がv2.6からv2.7へ改定されたが、
   Claude側は初回読み込みの版のまま作業を継続した（実害はなかった）
        ↓
④ カテゴリ・パーツ・断言をJSONで提案（既出照合込み／ハッシュ先頭12桁を併記）し、
   ルーティンの会話画面で返信を待つ
        ↓  CEOが会話画面を開き、内容を確認して「承認」と返信する
⑤ ルーティンが、ハッシュ付きJSONを `kamen-a-workflow` のブランチ
   `claude/qabox-pending` の `pending/YYYY-MM-DD_HHMM_<ハッシュ先頭12桁>.json` にpushする【v1.1改定】
   （GitHub連携の権限のみを使う。API_TOKENは使わない。mainへはpushしない）
        ↓  GASが1時間ごとに巡回（質問箱_pending反映.gs）
⑥ GASがハッシュを再計算して照合し、既存の反映処理（applyJson）で検証・反映する
     E〜K列が埋まり、断言台帳タブに行が追加され、処理状態が「済」になる
     Drive上の assertions_latest.csv も自動で更新される
     反映済みのファイルは pending/processed/、不合格のファイルは pending/failed/ へ移す
        ↓
⑦ GASがCEOに結果をメールで通知する（ハッシュ先頭12桁つき）
     CEOは④で見た12桁と一致しているかを確認する
```

- **③はCEOの明示指示を待たない。** 毎日9時にルーティンが起動した時点で、Claudeは取得と分類まで進める
- ルーティンはクラウド上で動くため、CEOのPCが起動している必要はない。CEOがその日に承認できなくても、会話画面は残るので後から返信すればよい。未処理分は翌日以降の提示にも合算される（取りこぼしにはならない。§6.6参照）
- **④の承認ゲートは廃止しない。** 無承認で断言台帳に書き込むことはしない
- **反映は承認から最大1時間遅れる。** 承認した時点ではまだ台帳に入っていない
- 分割して進めてよい。10件程度を1バッチとする。断言は1件あたり2〜6本のため、10件で40本前後になり、CEOが目を通せる分量に収まる

### 6.2 エンドポイント

接続情報は `kamen-a-workflow/.env`【v2.6改定】。キー構成は同リポジトリの `.env.example` を参照する（`WEB_APP_URL` / `API_TOKEN` / `SHEET_ID` / `QABOX_GID` / `RULES_DIR` / `DATA_DIR` ほか）。`.env` は `.gitignore` で保護されており、派生形（`.env.txt` 等）も除外対象である。実装の詳細は『仮面A パイプライン自動化 実装仕様書』に置く。

**接続情報を他のファイルへ複製しない**。旧 `pipeline_config.md` は同一の値を平文で保持し、かつgit管理外のフォルダに置かれていたため廃止した。

**`API_TOKEN` をローカル環境の外へ持ち出さない**【v2.8新設】。`.env` を読んで値を別の実行環境（クラウド上のシェル等）へ渡す運用は、経路や相手を問わず禁止する。ローカルシェルが使用不能な場合は**作業を中止し、CEOに報告する**。代替経路での続行は認めない。契機：2026-09-15、device_bashが不具合で使用不能だったため、`.env` の値を読み取ってクラウド環境からcurlを実行した事例が発生した。WEB_APP_URLが公開エンドポイントであることと、トークンが手元を離れることは別の問題である。

**クラウドルーティンは `.env` を持たない**【v1.1新設】。ルーティンの実行環境はリポジトリを新しく複製したものであり、ローカルの `.env` は存在しない。したがってルーティンは `API_TOKEN` を扱えず、上記の持ち出し禁止に構造的に抵触しない。承認後の反映（§6.1⑤〜⑥）はGitHubへのpushとGASの内部処理だけで完結する。

**ルーティン環境への `WEB_APP_URL` の配置を許可する**【v1.1新設・CEO承認】。上記「接続情報を他のファイルへ複製しない」の唯一の例外である。
- 置き場所は、ルーティン専用のクラウド環境 `kamen-a` の環境変数に限る
- 置いてよいのは `WEB_APP_URL` のみ。**`API_TOKEN` は置かない**
- 根拠：GETは無認証で読める設計である。ただし `ledger` には未公開の【仮説】【未確認】が含まれ、URLを知られないことが唯一の防御である点は変わらない。環境変数は、その環境の利用者（CEO本人のみ）にしか見えない
- ルーティンは `parts.csv` を、`PARTS_CSV_URL` の代わりに raw URL（`inagaki-kado/ibs_mini`）から直接取得する。公開リポジトリのURLであり、秘密情報ではない

**GASがGitHubを読み書きする鍵は `GITHUB_TOKEN`**【v1.1新設】である。対象は `kamen-a-workflow` のみ、権限はContentsの読み書きのみ。GASのスクリプトプロパティにだけ置く。`API_TOKEN` とは別物である。

| 種別 | 用途 |
|---|---|
| `GET ?type=qabox_pending` | 未処理分の取得。`limit` `offset` で分割できる |
| `GET ?type=ledger` | 断言台帳CSV（§1 ①.0.2） |
| `GET ?type=health` | 診断。未処理件数・台帳行数・イベント台帳の鮮度 |
| `POST ?type=apply` | 分類JSONの反映。トークン必須。**v1.1以降は手動退避路**（§6.7） |

**curlでPOSTする際に `-X POST` を付けてはならない。** Apps Scriptは302で結果URLへ誘導するため、
メソッドを固定すると405になる。`--data-binary` のみで送ること。

```bash
curl -sL -H "Content-Type: application/json" \
     --data-binary @post.json "$WEB_APP_URL?token=$API_TOKEN"
```

`qabox_pending` は、シートに `\n` リテラルで格納された回答本文を**実改行に復元して返す**。
復元されていない場合はエンドポイントの不具合であり、そのまま分類してはならない。

### 6.3 自動取り込みの仕様（Apps Script）

- 対象：`https://note.com/qa/kamen_a` の公開ページ。生HTMLに最新20件の
  個別URL・質問全文・回答全文・公開日時（ISO8601）がserver-rendered DOMとして含まれる
- 実行：毎日4時台。1日1リクエスト
- 重複判定：質問URL（32桁ハッシュ）。既存行と一致すればスキップする
- 採番：**公開日の昇順（No.1が最古）**。複数件を同時に取り込む場合も必ず昇順に並べてから
  最大No+1を付与する。HTMLの表示順（新しい順）をそのまま使ってはならない
- D列の改行は `\n` リテラルで格納する（既存385行と同形式）。
  KAMEN Ace LABアプリ側で実改行へ復元して表示している
- **1日の新規回答が20件を超えると溢れた分は取り込めない。** 想定運用では問題ないが、
  大量公開した場合は「いま取り込む」を当日中に手動実行すること
- 健全性チェック：メニュー「パーサーを自己診断」、または `?type=health`。
  noteのリニューアルでHTML構造が変わると抽出0件になる。その場合はパーサーの修正が必要

### 6.4 Claudeが出力するJSON

```json
{
  "type": "apply",
  "payload_hash": "sha256:...（64桁）",
  "payload": {
    "questions": [
      {"no":"386","category":"カテゴリ名","blades":"","ratchets":"","bits":"","lockchips":"","overs":"","assists":""}
    ],
    "assertions": [
      {"no":"386","claim":"断言の全文。","label":"【物理推論】","note":"根拠の出所。"}
    ]
  }
}
```

**出典記事・公開日・備考末尾の質問URLは出力しない。** スクリプトが自動生成する。
これにより、過去に事故った全角コロン（`質問箱：`）の表記ゆれと質問URLの欠落が
構造的に起きなくなった。

**ハッシュによる承認内容の担保**【v2.0新設】

- `payload_hash` は `payload` のみを対象とする。キーを再帰的に昇順ソートし、
  区切り文字なしのminified JSONにしてUTF-8でSHA-256を取る
- **Claudeは分類結果をCEOに提示する際、ハッシュ先頭12桁を必ず併記する**
- GASは受信payloadからハッシュを再計算し、不一致・欠落なら**何も書き込まず**400を返す
- レスポンスに受理したハッシュが含まれる。CEOは提示時の値と目視照合できる
- 承認後にClaudeが内容を変更すればハッシュが変わるため、必ず検出される

**二重送信の防止**：同一ハッシュを24時間以内に再受信した場合、書き込まず `duplicate: true` を返す。
通信エラーからの再送で断言が二重登録されることはない。
GitHub経由の反映では、反映したハッシュを**期限なしで**記録する。記録済みのファイルは書き込まず processed/ へ移すだけにする。反映の記録はファイル移動より先に行うため、移動に失敗しても二重反映は起きない【v1.1新設】。

検証は従来どおりである。カテゴリが既定9種以外、確度ラベルが規定外、
断言や備考が空、存在しないNo、questions内のNo重複、**断言のNoが questions に含まれていない**【v1.1新設】——いずれか1つでも見つかれば**何も書き込まずエラー一覧を返す**。
最後の検証は、処理状態が「済」にならないまま断言だけが台帳に入り、翌日に同じ断言が再提案されて二重登録される事故を防ぐためのものである。
中途半端な反映は起きない。

### 6.5 各項目の記載ルール

- **カテゴリ**：質問箱CSVリスト生成仕様書§1の**既定9種のみ**から選択。複数は `;` 区切り。
  新カテゴリの追加はCEO承認を要する
- **パーツ列**：parts.csvの表記に厳密に従う（取得は①.0.4）。複数は `;` 区切り。該当なしは空文字
  - `overs`（オーバーブレード）と `assists`（アシストブレード）は略号が重複する（`F` `B` 等）。
    略号だけで判別せず、必ず種別で振り分ける。CX表記のパースは1文字目＝オーバー、2文字目＝アシスト
  - 既知の罠：ローラッシュ（×ロウラッシュ）／シャークスケイル（×シャークエッジ）／
    ワイバーンホバー（×ワイバーンゲイル）／ティラノビート（×タイラノビート）
- **断言**：回答本文から、検証可能な主張文のみを抜き出す。締め文・質問者への呼びかけ・修辞は
  対象外。原文の表現を尊重しつつ、単独で意味が通る文に整形してよい。1回答あたり2〜6本が目安
- **確度ラベル**：規定5種、または「【確認済み（出典名）】」形式。**台帳では全ラベルを省略せず明示する**。
  §2の非表示ルールは公開文のみに適用し、転記元は剥離前の原稿とする
- **備考**：根拠の出所を簡潔に。一次ソース名／実測ファイル名／物理式／データ集計範囲のいずれか。
  空欄にしない
- **断言があるNoは、必ず questions にも含める**【v1.1新設】
- 既出断言と重複する内容は追記しない。既存台帳の方が出典が明確な場合は、そちらを正とする
- 既出断言と矛盾する断言を見つけた場合、**サイレント修正するな。** JSONから除外し、
  CEOに報告して判断を仰ぐ
  - **【2026-09-18改定】報告した先に経路ができた。** CEOが承認した場合に限り、
    §6.9の `revise` で既存行を訂正できる。**承認なしの修正は引き続き禁止である。**
    `apply`（本節）は新規追記専用であり、既存行には触れない

### 6.6 定期タスク

| タスク | タイミング | 内容 |
|---|---|---|
| 【毎日9時】質問箱 未処理分の分類・提案 | 毎日9時（Codeのクラウドルーティン「質問箱 断言台帳 反映」）【v1.1改定】 | §6.1の③〜⑤を行う。前日以前の未処理分が残っている場合は、当日分と合算してまとめて提示する |
| 【1時間ごと】承認済みファイルの反映 | 1時間ごと（GASの時間主導トリガー）【v1.1新設】 | §6.1の⑥〜⑦を行う。承認済みのファイルが無ければ何もしない |

**CEOがその日に確認できなくても取りこぼしにはならない。** `?type=qabox_pending` は処理状態=未処理の行を
日付を問わず全件返すため、未確認日が続いても翌日以降に合算されるだけである。ただし放置期間が長引くと
1回あたりの提示件数が増える（§6.1末尾の10件/バッチの目安を超える場合は複数回に分けて進める）。

**chatのスケジュール機能による定期タスクはv1.1で廃止した。** 起動元をCodeのクラウドルーティンに一本化した。
ルーティンの設定（環境 `kamen-a`、リポジトリ `kamen-a-workflow`・`kamen-a-rules`、コネクタなし）は変更しないこと。

**第1・第3水曜のCSV同期タスクはv2.0で廃止した。** 断言台帳はWeb Appから都度取得するため、
プロジェクトファイルへの差し替え作業は存在しない。

毎回ゼロから手順を説明する。CEOは運用を忘れやすいと自認しているため、
「前回と同じです」で省略しない。

### 6.7 手動退避路

ルーティンやGitHub経由の反映が止まった場合に備え、次の2つを残してある。**常用しないこと。**
- ローカルの `kamen-a-workflow` から `POST ?type=apply` を送る（ハッシュ検証あり）
- メニュー「質問箱 → 反映する」にJSONを直接貼り付ける（ハッシュ検証なし。承認内容の担保が働かない）

承認済みファイルをすぐ反映したいときは、メニュー「質問箱 → 承認済みpendingをいま反映」を使う（1時間待たずに⑥を実行する）【v1.1新設】。

メニュー「未処理を書き出す」はv2.0で撤去し、`exportPending` 関数もv1.1で削除した【v1.1訂正】。
`?type=qabox_pending` が使うのは独立した関数 `readPending_` と `dec_` であり、これらは残してある。
（v1.0までの「`readPending_` が参照されているため `exportPending` を残してある」という記述は誤りだった）

### 6.9 revise——既存行の訂正【2026-09-18新設】

`apply` は新規追記専用で、既存行を指定して書き換える形になっていない。
**既存行の訂正・失効・重複削除のために `revise` を新設した**（2026-09-18・CEO判断）。

契機：パーツDBの全件転記の過程で台帳側の誤り・失効・重複が複数見つかったが、
直す経路が無かった。規則は『パーツ研究仕様書』§6が正本である。

```json
{
  "type": "revise",
  "payload_hash": "sha256:...（64桁）",
  "payload": {
    "revisions": [
      {"row": 555,
       "expect_claim_head": "競技でよく使われるビットのバースト耐性公称値は、",
       "op": "annotate",
       "note_append": "【訂正 2026-09-18】Ballのバースト耐性は30。..."},
      {"row": 1291,
       "expect_claim_head": "バレットグリフォンの本体はラチェットが存在しない",
       "op": "correct",
       "claim": "バレットグリフォンはラチェットが一体化して交換できない構造であり、...",
       "label": "【確認済み】",
       "note_append": "【訂正 2026-09-18】..."},
      {"row": 2015,
       "expect_claim_head": "バレットグリフォンはラチェットが存在しない固定式構造で",
       "op": "delete_dup",
       "keep_row": 1977}
    ]
  }
}
```

**op は4種のみ**（危険度と安全装置は『パーツ研究仕様書』§6.3の表が正本）。

| op | 必須フィールド | 動作 |
|---|---|---|
| `annotate` | `note_append` | 備考に追記。断言・ラベルは触らない |
| `expire` | `note_append` | 備考に `【失効】` と理由を追記。断言は残す |
| `correct` | `claim`（または `label`）＋`note_append` | 断言・ラベルを書き換え、**旧文を備考へ退避** |
| `delete_dup` | `keep_row` | 重複行を削除。残す行を明示させる |

**GAS側の実装**

ハンドラは `API_doRevise_(payload)` とし、**`payload` だけを受け取って
`{ok: true/false, ...}` のプレーンなオブジェクトを返す。**
`doPost` が `API_json_` で包むため、`ContentService` を返してはならない。

**トークン検証・`payload_hash` の再計算と照合・24時間の二重送信防止・成功時のみのハッシュ記録は、
すべて `doPost` が集約して行っている。** ハンドラ側で重複させないこと。
ディスパッチは `doPost` の既存の分岐に1行足すだけである。

```javascript
else if (type === 'revise')        res = API_doRevise_(body.payload);
```

**ハンドラが強制する検証**

1. `row` が実在し、**現在の断言が `expect_claim_head`（20文字以上）で始まる**こと。
   不一致なら**何も書き込まない**。行番号は位置依存なので行番号だけを信頼しない
2. `op` が4種のいずれかで、必須フィールドが揃っていること
3. `correct` は**旧断言・旧ラベルを備考へ退避**してから書き換える（監査記録を失わない）
4. `delete_dup` は `keep_row` と**断言・ラベル・出典記事・公開日が完全一致**すること
5. **全件成功か全件不実行。** 1件でも落ちたら何も書き込まない
6. **備考に `[parts_db由来 YYYY-MM-DD]` を機構側で付与する。**
   Claudeが書くのではなくGASが付ける（『パーツ研究仕様書』§6.2の循環参照ガード）
7. 書き込み後に `API_exportLedgerToDrive_()` を呼び、`apply` と同様に
   Drive上の `assertions_latest.csv` を更新する

**行番号の前提：** `?type=ledger` が返すCSVは `API_buildLedgerCsv_()` が
**A列が空の行を除外して**作る。空行が無い限りCSVの行番号とシートの行番号は一致し、
`ledger.py grep` の表示行番号がそのまま `row` に使える。
**A列が空の行ができると両者がズレる。** 訂正前に `health` の行数と
取得したCSVの行数が一致することを確認すること。

**削除は行を物理削除するため行番号が繰り上がる。**
`delete_dup` を含むpayloadを送ったあとは、**必ず `health` で行数を確認し、
以後の訂正は取得し直した台帳の行番号で組み立てること。**
同一payload内で複数の `delete_dup` を送る場合、GASは**行番号の大きい順に処理する**。

### 6.10 公開後に回答を訂正したときの後処理【2026-09-25新設】

**noteの回答を同じURLのまま差し替えても、「質問箱」タブは追従しない。**
§6.3の自動取り込みは質問URLで重複を判定し、既存行をスキップする。
差し替えた回答は取り込まれず、D列（回答）には訂正前の本文が残る。
このタブは過去回答の正本（『質問箱ワークフロー仕様書』§1 ①.0.3）なので、
放置すると次の回答が訂正前の誤りを引く。

訂正を公開したら、次の4つを必ず行う。

| # | 作業 | 担当 | 置き場所・手段 |
|---|---|---|---|
| 1 | 公開版の保存 | Claude | `tasks/<id>/published.md`（訂正版）。初回公開版が手元に無ければ `published_v1.md` として残す。`state.json` に `corrected` を記録 |
| 2 | 訂正ログへの記録 | Claude | `ベイブレードAI/訂正ログ.md`。誤／正／原因／指摘者クレジット／併せて対応／再発防止 |
| 3 | 断言台帳の訂正 | Claude（CEO承認） | 台帳を出典記事列（`質問箱：<質問文の先頭>`）と誤った主張の語句の両方でgrepし、該当行を§6.9の `revise` で直す。回答から消した主張は `expire`、書き換えた主張は `correct`。同じ主張が記事由来の行にもあれば、主・副の扱いで `correct`（ラベル降格）か `annotate` |
| 4 | 質問箱タブD列の貼り替え | **CEO（手作業）** | ClaudeがD列用のテキスト（改行を `\n` リテラルに変換済み）を渡し、CEOが該当NoのD列に貼る。書き込み用のAPIは無い。E〜K列・処理状態は触らない |

- **3は、誤った主張が台帳に無ければ不要。** grepで無いことを確かめ、その旨を訂正ログに書く
- **4の後、Claudeは `ledger.py qabox --fresh` で取り直し、**新しい本文があること・誤った文が
  消えていることを確かめる。訂正注記の中で旧文を引用している場合は、その引用は残ってよい
- 訂正の文面（本文の差し替え・注記・Xの訂正ポスト）は公開前にCEOが確定する。公開済みの訂正はCEO判断である

契機：2026-09-25、SNSで指摘を受けた質問箱回答2件（No.463・No.477）を同じURLのまま訂正した。
断言台帳は `revise` で直したが、質問箱タブのD列は旧本文のまま残っていた。

### 6.8 本仕様書の範囲外

記事（note.com/kamen_a）由来の断言の取り込みは、本仕様書の対象外である。
以下は別途『記事断言取り込み仕様書』に定める（2026-09-12時点で未策定）。

- 記事本文の取得と改訂検知（本文ハッシュによる再キューイング）
- WBOレポート由来の断言の選別（順位・スコアのスナップショットは台帳に登録せず、
  集計方法の定義・物理的考察・パーツ特性の恒久的断言のみを対象とする）
- 記事タイトルの命名規則（2026-09-15以降適用）

記事を公開した場合、当面は従来どおり断言台帳の更新プロンプトを手動で回す。
